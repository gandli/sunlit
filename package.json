{
  "name": "sunlit-solid",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sunlit — SolidJS implementation. See SPEC.md on the `spec` branch for the cross-branch contract.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "vitest run tests/unit",
    "test:e2e": "playwright test tests/e2e"
  },
  "dependencies": {
    "solid-js": "^1.9.5"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@solidjs/testing-library": "^0.8.10",
    "@testing-library/jest-dom": "^6.4.5",
    "jsdom": "^25.0.0",
    "vite": "^7.0.0",
    "vite-plugin-solid": "^2.11.6",
    "vitest": "^3.0.0"
  }
}
