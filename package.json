{
  "name": "sunlit-svelte",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sunlit — Svelte 5 (Runes) implementation. See SPEC.md on the `spec` branch for the cross-branch contract.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "vitest run tests/unit",
    "test:e2e": "playwright test tests/e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@sveltejs/vite-plugin-svelte": "^6.2.4",
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/svelte": "^5.2.4",
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^25.0.0",
    "svelte": "^5.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
