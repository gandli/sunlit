{
  "name": "sunlit-preact",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Sunlit — Preact 10 implementation via preact/compat. See SPEC.md on the `spec` branch.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "vitest run tests/unit",
    "test:e2e": "playwright test tests/e2e"
  },
  "dependencies": {
    "preact": "^10.24.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@preact/preset-vite": "^2.9.1",
    "@testing-library/jest-dom": "^6.4.5",
    "@testing-library/preact": "^3.2.4",
    "jsdom": "^25.0.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
